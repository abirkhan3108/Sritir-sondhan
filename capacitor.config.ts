import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sritirsondhan.admin',
  appName: 'স্মৃতি সংরক্ষণ Admin',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
