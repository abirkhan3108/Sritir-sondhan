{
  "name": "smriti-sangrokkhon",
  "version": "1.0.0",
  "private": true,
  "description": "স্মৃতি সংরক্ষণ",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p dist && cp -r *.html *.js *.css assets public dist 2>/dev/null || true",
    "build:web": "npm run build",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3",
    "@capacitor/android": "^7.4.3",
    "typescript": "^5.8.3"
  }
}
