{
  "name": "smriti-sangrokkhon",
  "version": "1.0.0",
  "private": true,
  "description": "স্মৃতি সংরক্ষণ",
  "scripts": {
    "build": "rm -rf www && mkdir -p www && find . -maxdepth 1 -type f \\( -name '*.html' -o -name '*.js' -o -name '*.css' \\) -exec cp {} www/ \\; && if [ -d assets ]; then cp -r assets www/; fi && if [ -d public ]; then cp -r public www/; fi",
    "build:web": "npm run build",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3",
    "@capacitor/android": "^7.4.3",
    "typescript": "^5.8.3"
  }
}
